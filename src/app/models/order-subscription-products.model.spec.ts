import { OrderSubscriptionProducts } from './order-subscription-products.model';

describe('OrderSubscriptionProducts', () => {
  it('should create an instance', () => {
    expect(new OrderSubscriptionProducts()).toBeTruthy();
  });
});
