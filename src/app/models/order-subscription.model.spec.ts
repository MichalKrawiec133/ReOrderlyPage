import { OrderSubscription } from './order-subscription.model';

describe('OrderSubscription', () => {
  it('should create an instance', () => {
    expect(new OrderSubscription()).toBeTruthy();
  });
});
